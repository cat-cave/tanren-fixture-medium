version: 1
bootstrap:
  run: "npm install --no-package-lock --no-audit --no-fund"
tiers:
  fast:
    - name: typecheck
      run: "npx tsc --noEmit -p tsconfig.json"
  slow:
    - name: test
      run: "npm test"
when:
  fast:
    - per_iteration
  slow:
    - pre_audit
    - pre_merge
